import 'package:flutter/material.dart';
import 'package:get/get.dart';

class add_client extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return  Scaffold(
      appBar: AppBar(
        title: const Text('Add CLient Page'),
      ),
      body: const Center(
        child: Text('Receipt Page'),
      ),
    );
  }
}